import { requireAdmin } from "@/lib/auth";
export default async function SettingsPage() { const admin = await requireAdmin(); return <main className="admin-page"><div className="shell"><div className="admin-top"><div><p className="eyebrow">Workspace</p><h1>Settings</h1></div></div><section className="admin-table-wrap settings-panel"><div className="admin-section-head"><div><p className="eyebrow">Account</p><h2>Administrator profile</h2></div></div><div className="settings-content"><p><strong>{admin.name || "Administrator"}</strong><span>{admin.email}</span></p><p className="settings-note">Environment secrets and database credentials are kept server-side and are never displayed here.</p></div></section></div></main>; }
