import { updateApplicationStatus } from "../actions";
import { requireAdmin } from "@/lib/auth";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function ApplicationsPage() { await requireAdmin(); const applications = await db.application.findMany({ include: { student: true, batch: { include: { course: true } } }, orderBy: { appliedAt: "desc" } }); return <main className="admin-page"><div className="shell"><div className="admin-top"><div><p className="eyebrow">Admissions</p><h1>Applications</h1></div></div><div className="admin-table-wrap"><div className="admin-section-head"><div><p className="eyebrow">Review queue</p><h2>{applications.length} applications</h2></div></div><div className="admin-table-scroll"><table><thead><tr><th>Student</th><th>Course / batch</th><th>Applied</th><th>Status</th><th>Update</th></tr></thead><tbody>{applications.map((application) => <tr key={application.id}><td><strong>{application.student.fullName}</strong><small>{application.student.phone} · {application.student.email}</small></td><td>{application.batch.course.name}<small>{application.batch.identifier} · {application.batch.startTime}</small></td><td>{application.appliedAt.toLocaleDateString("en-GB")}</td><td>{application.status}</td><td><form action={updateApplicationStatus} className="status-form"><input type="hidden" name="id" value={application.id} /><select name="status" defaultValue={application.status}><option>PENDING</option><option>CONFIRMED</option><option>REJECTED</option><option>CANCELLED</option><option>WAITLISTED</option></select><button type="submit">Save</button></form></td></tr>)}</tbody></table></div></div></div></main>; }
