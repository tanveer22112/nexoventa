import { createCourse, toggleCourse } from "../actions";
import { requireAdmin } from "@/lib/auth";
import { db } from "@/lib/db";

export const dynamic = "force-dynamic";
export default async function CoursesPage() { await requireAdmin(); const courses = await db.course.findMany({ include: { _count: { select: { batches: true } } }, orderBy: { createdAt: "desc" } }); return <AdminPage title="Courses" eyebrow="Catalog"><form action={createCourse} className="admin-form"><Field label="Course name" name="name" /><Field label="Slug" name="slug" /><Field label="Duration" name="duration" /><Field label="Description" name="description" /><button className="admin-action primary" type="submit">Create course</button></form><div className="admin-table-wrap"><div className="admin-section-head"><div><p className="eyebrow">Course catalog</p><h2>All courses</h2></div></div><div className="admin-table-scroll"><table><thead><tr><th>Name</th><th>Slug</th><th>Batches</th><th>Status</th><th>Action</th></tr></thead><tbody>{courses.map((course) => <tr key={course.id}><td><strong>{course.name}</strong><small>{course.description}</small></td><td>{course.slug}</td><td>{course._count.batches}</td><td>{course.active ? "Active" : "Inactive"}</td><td><form action={toggleCourse}><input type="hidden" name="id" value={course.id} /><button className="admin-action" type="submit">{course.active ? "Deactivate" : "Activate"}</button></form></td></tr>)}</tbody></table></div></div></AdminPage>; }
function Field({ label, name }: { label: string; name: string }) { return <label className="field"><span>{label}</span><input name={name} required /></label>; }
function AdminPage({ title, eyebrow, children }: { title: string; eyebrow: string; children: React.ReactNode }) { return <main className="admin-page"><div className="shell"><div className="admin-top"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1></div></div>{children}</div></main>; }
