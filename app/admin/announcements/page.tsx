import { createAnnouncement, toggleAnnouncement } from "../actions";
import { requireAdmin } from "@/lib/auth";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function AnnouncementsPage() { await requireAdmin(); const announcements = await db.announcement.findMany({ orderBy: { createdAt: "desc" } }); return <main className="admin-page"><div className="shell"><div className="admin-top"><div><p className="eyebrow">Public updates</p><h1>Announcements</h1></div></div><form action={createAnnouncement} className="admin-form"><Field label="Title" name="title" /><Field label="Slug" name="slug" /><label className="field"><span>Content</span><textarea name="content" required rows={3} /></label><button className="admin-action primary" type="submit">Create announcement</button></form><div className="admin-table-wrap"><div className="admin-section-head"><div><p className="eyebrow">Editorial queue</p><h2>All announcements</h2></div></div><div className="admin-table-scroll"><table><thead><tr><th>Title</th><th>Slug</th><th>Status</th><th>Action</th></tr></thead><tbody>{announcements.map((item) => <tr key={item.id}><td><strong>{item.title}</strong><small>{item.content}</small></td><td>{item.slug}</td><td>{item.published ? "Published" : "Draft"}</td><td><form action={toggleAnnouncement}><input type="hidden" name="id" value={item.id} /><button className="admin-action" type="submit">{item.published ? "Unpublish" : "Publish"}</button></form></td></tr>)}</tbody></table></div></div></div></main>; }
function Field({ label, name }: { label: string; name: string }) { return <label className="field"><span>{label}</span><input name={name} required /></label>; }
