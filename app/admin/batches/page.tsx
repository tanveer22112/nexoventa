import { createBatch, updateBatchStatus } from "../actions";
import { requireAdmin } from "@/lib/auth";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function BatchesPage() { await requireAdmin(); const [courses, batches] = await Promise.all([db.course.findMany({ where: { active: true }, orderBy: { name: "asc" } }), db.batch.findMany({ include: { course: true }, orderBy: [{ year: "desc" }, { month: "desc" }, { startTime: "asc" }] })]); return <main className="admin-page"><div className="shell"><div className="admin-top"><div><p className="eyebrow">Training operations</p><h1>Batches</h1></div></div><form action={createBatch} className="admin-form"><label className="field"><span>Course</span><select name="courseId" required><option value="">Select course</option>{courses.map((course) => <option key={course.id} value={course.id}>{course.name}</option>)}</select></label><Field label="Identifier" name="identifier" /><Field label="Month" name="month" type="number" /><Field label="Year" name="year" type="number" /><Field label="Days" name="daysOfWeek" /><Field label="Start" name="startTime" /><Field label="End" name="endTime" /><Field label="Capacity" name="capacity" type="number" /><button className="admin-action primary" type="submit">Create batch</button></form><div className="admin-table-wrap"><div className="admin-section-head"><div><p className="eyebrow">Schedule</p><h2>All batches</h2></div></div><div className="admin-table-scroll"><table><thead><tr><th>Batch</th><th>Schedule</th><th>Seats</th><th>Status</th><th>Update</th></tr></thead><tbody>{batches.map((batch) => <tr key={batch.id}><td><strong>{batch.course.name}</strong><small>{batch.identifier} · {batch.month}/{batch.year}</small></td><td>{batch.daysOfWeek}<small>{batch.startTime} – {batch.endTime}</small></td><td>{batch.reservedSeats} / {batch.capacity}<small>{Math.max(batch.capacity - batch.reservedSeats, 0)} available</small></td><td>{batch.status}</td><td><form action={updateBatchStatus} className="status-form"><input type="hidden" name="id" value={batch.id} /><select name="status" defaultValue={batch.status}><option>DRAFT</option><option>OPEN</option><option>CLOSED</option><option>ARCHIVED</option></select><button type="submit">Save</button></form></td></tr>)}</tbody></table></div></div></div></main>; }
function Field({ label, name, type = "text" }: { label: string; name: string; type?: string }) { return <label className="field"><span>{label}</span><input name={name} type={type} required /></label>; }
